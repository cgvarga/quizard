import React, { useState, useEffect } from 'react';

const Quiz = () => {
	// const { questions } = quiz;
	return (
		<div>
			{/* {questions.map((question) => (
				<p>{questions.value}</p>
			))} */}
			<p>Sample Quiz</p>
		</div>
	);
};

export default Quiz;
